{
  "name": "parcel-vanilla-starter",
  "version": "0.0.0",
  "private": true,
  "source": "src/index.html",
  "scripts": {
    "build": "rimraf dist && NODE_ENV=production parcel build",
    "dev": "NODE_ENV=test parcel"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.14.3",
    "autoprefixer": "^10.4.21",
    "parcel": "^2.14.0",
    "postcss": "^8.2.1",
    "postcss-skin-peeler": "workspace:*",
    "rimraf": "^6.0.1"
  }
}
